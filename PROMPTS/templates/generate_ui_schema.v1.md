# Prompt Template: generate_ui_schema (v1)

> プレースホルダー。実際の変数展開ロジックは `backend/app/ai/generators/` 側で実装する。

入力:
- ユーザーの会話履歴
- 直近の `app_versions`（存在する場合）

出力:
- `shared/schemas/ui_schema.v1.json` に準拠したJSON UI Schema 1件
